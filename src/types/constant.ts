export declare const level: {
  error: number;
  warn: number;
  info: number;
  http: number;
  debug: number;
};

export enum colors {
  error = 'red',
  warn = 'yellow',
  info = 'green',
  http = 'magenta',
  debug = 'white',
}
